# ESP32 蓝牙控制指令说明文档

本文档详细描述了通过蓝牙连接控制 ESP32 设备的所有可用指令及其功能。

## 连接方式

ESP32 通过串口通信接收蓝牙指令。设备会监听 Serial2 (波特率 9600) 上的数据，解析收到的指令并执行相应的操作。

## 指令格式

所有指令均为文本字符串，以换行符 `\n` 结尾。指令不区分大小写。

## 控制指令列表

### LED 控制指令

| 指令      | 功能         | 说明                     |
|-----------|--------------|--------------------------|
| `on`      | 打开 LED1    | 点亮 LED1 灯             |
| `off`     | 关闭 LED1    | 熄灭 LED1 灯             |

### 状态播报指令

这些指令会触发设备播放预设的状态语音提示：

| 指令       | 功能             | 语音内容               |
|------------|------------------|------------------------|
| `status1`  | 播报状态1        | "您已超速请减速慢行"   |
| `status2`  | 播报状态2        | "请注意转弯"           |
| `status3`  | 播报状态3        | "请注意倒车"           |
| `status4`  | 播报状态4        | "电池电量低"           |

### 音乐播放控制指令

| 指令            | 功能           | 说明                                   |
|-----------------|----------------|----------------------------------------|
| `play`          | 播放音乐       | 播放预设播放列表中的当前歌曲           |
| `stop`          | 停止播放       | 停止当前正在播放的音乐                 |
| `next_song`     | 下一首         | 切换到播放列表中的下一首歌曲           |
| `previous_song` | 上一首         | 切换到播放列表中的上一首歌曲           |
| `pause/resume`  | 暂停/恢复播放  | 在播放和暂停状态之间切换               |
| `volumeup`      | 增加音量       | 将音量增加一级                         |
| `volumedown`    | 减少音量       | 将音量减少一级                         |

### 语音合成参数设置指令

可以通过以下指令调整语音合成(TTS)的效果：

| 指令格式        | 功能         | 参数范围 | 默认值 | 说明                       |
|-----------------|--------------|----------|--------|----------------------------|
| `SET_PER:x`     | 设置发音人   | 0-4      | 3      | 选择不同的发音人声音       |
| `SET_SPD:x`     | 设置语速     | 0-15     | 1      | 调整语音播放速度           |
| `SET_PIT:x`     | 设置音调     | 0-15     | 3      | 调整语音音调高低           |
| `SET_VOL:x`     | 设置音量     | 0-15     | 5      | 调整语音播放音量           |

示例：
- `SET_PER:4` - 设置发音人为编号4的声音
- `SET_SPD:7` - 设置语速为7
- `SET_PIT:6` - 设置音调为6
- `SET_VOL:9` - 设置音量为9

### 播放列表管理指令

设备支持动态管理播放列表：

| 指令格式              | 功能               | 说明                                           |
|-----------------------|--------------------|------------------------------------------------|
| `ADDURL:<url>`        | 添加歌曲URL        | 将指定URL添加到播放列表末尾                    |
| `PLAYLIST:[<json>]`   | 设置播放列表       | 用JSON格式的URL数组替换当前播放列表            |
| `PLAYLIST:<index>`    | 播放指定索引歌曲   | 播放播放列表中指定索引位置的歌曲               |
| `LISTSHOW`            | 显示播放列表       | 在串口监视器中打印当前播放列表内容             |
| `LISTCLEAR`           | 清空播放列表       | 移除播放列表中的所有歌曲                       |

示例：
- `ADDURL:http://example.com/song.mp3` - 添加歌曲
- `PLAYLIST:["http://song1.mp3","http://song2.mp3"]` - 设置新的播放列表
- `PLAYLIST:2` - 播放播放列表中索引为2的歌曲（索引从0开始）

### 速度监控设置指令

| 指令格式         | 功能              | 说明                                        |
|------------------|-------------------|---------------------------------------------|
| `A=<value>`      | 设置速度阈值      | 设置超速警告的速度阈值，单位为km/h          |


示例：
- `A=60` - 设置速度阈值为60km/h


## 使用示例

1. 发送 `on` 指令点亮 LED1
2. 发送 `status1` 播报超速警告
3. 发送 `play` 开始播放音乐
4. 发送 `volumeup` 增加音量
5. 发送 `SET_PER:4` 更改发音人
6. 发送 `A=50` 设置超速阈值为50km/h

## 技术细节

- 所有语音播报基于百度语音合成服务
- 语音识别基于百度语音识别服务
- 状态播报会在播放前自动暂停音乐，播报完成后恢复播放
- 音乐播放使用 I2S 接口输出音频
- 支持最多50首歌曲的播放列表管理