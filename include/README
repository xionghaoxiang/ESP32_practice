
该目录用于存放项目头文件。

头文件是包含 C 声明和宏定义的文件，用于在多个项目源文件之间共享。你可以在 `src` 文件夹中的项目源文件（如 C、C++ 等）中通过 C 预处理指令 `#include` 来引用头文件。

```src/main.c

#include "header.h"

int main (void)
{
 ...
}
```

包含头文件的效果等同于将头文件内容复制到每个需要它的源文件中。手动复制既耗时又容易出错。使用头文件，相关声明只需放在一个地方。如果需要修改，只需在一个地方修改，所有包含该头文件的程序在下次编译时会自动使用新版本。头文件避免了查找和修改所有副本的繁琐工作，也减少了因遗漏副本导致程序不一致的风险。

在 C 语言中，头文件通常以 `.h` 结尾。

更多关于头文件的使用方法，请参考官方 GCC 文档：

* 包含语法
* 包含操作
* 仅包含一次的头文件
* 计算包含

https://gcc.gnu.org/onlinedocs/cpp/Header-Files.html

